import React from 'react';
import { useSelector } from 'react-redux';
import ReactHtmlParser from 'react-html-parser';

import { map_contact, map_title, map_address }
    from './curriculumSlice';

export default function Header () {
	const title = useSelector(map_title),
          address = useSelector(map_address),
          contact = useSelector(map_contact);

    return (
        <header className="home">
			<h1>riddle</h1>
            class HomePage extends Component {
  render() 

      <article>
        <div>
          <section>
            <h1>Build the community your fans will love</h1>
            <h4> Riddles re-imagines the way we build communities. you have a voice but so does your audience. create connections with your users as you engage in genuine discussion</h4>
               <div>
                 <Link to="/">riddle</Link>
               </div>
               <div>
                 
                 <Link to="/register">Register</Link>
               </div>
          </section>
        </div>
      </article>
    );
  }




            
